# Verification for Nova Blocks issues #620–#624

QA date: 18 September 2026. Source: [455bd998](https://github.com/pixelgrade/nova-blocks/commit/455bd9989615c57c8b3020340179e36814ea8f33) on main.

Live checks used an isolated source-mirrored WordPress Studio site with WordPress 7.1.1, Anima LT 2.0.49, Pixelgrade Assistant, Style Manager and the source-built Nova Blocks plugin. The existing Local site was unavailable. Screenshots are unedited browser captures of QA fixtures; #622's first screenshot is explicitly a rendered report of collected CLI/editor/ability data. The accompanying JSON contains measurements. Fixtures used the real WordPress block serializer.

| Issue | Behavior and live verification | Measurements |
| --- | --- | --- |
| [#620](https://github.com/pixelgrade/nova-blocks/issues/620) | Desktop limit400/mobile200; defaults30/24. 72 frontend cases: three image shapes × two logo types × three size pairs × four widths1440/1024/390/320. Image proportions, effective visibility, foreground hit-testing, containment and no horizontal overflow. Another15 sticky/clone/Patch cases each check load/scroll/return; ten include mobile menu cycles. Both editors check limits, individual undo, save/reload and default omission. Twelve extra wide/tall editor cases verify resize-box/row bounds and standalone core Logo isolation. | [Geometry](results/620-front-results.json), [sticky/mobile](results/620-sticky-results.json), [editor controls](results/620-editor-results.json), [wide/tall editor cases](results/620-editor-shapes-results.json) |
| [#621](https://github.com/pixelgrade/nova-blocks/issues/621) | Five per-instance text policies. Normal/Patch headers × global on/off and missing title/tagline/both = ten distinct scenarios, plus two restored-state repeats:60 policy outputs. Explicit text is unclipped and receives foreground hit-testing. Both editors verify SSR, undo, each policy and save/reload; Inherit omits its default attribute. Global settings stay unchanged when a local policy is chosen. | [Frontend policies](results/621-front-results.json), [editors](results/621-editor-results.json) |
| [#622](https://github.com/pixelgrade/nova-blocks/issues/622) | Six blocks: Group, Button, Separator, Columns, Column and List. Actual CLI schema/default/vocabulary matches editor registrations and the registered Blocks Describe ability. PHP registry remains unchanged. Six canonicalized fixtures preserve raw bytes and validate. Functional palette IDs are excluded when a block does not support them. Default-only Button/Separator schema overrides retain their actual missing type. | [CLI output](results/622-cli-results.json), [editor/ability/registry](results/622-live-results.json), [canonicalization](results/622-canonical-results.json), [Header Row capability](results/622-header-row-after.json) |
| [#623](https://github.com/pixelgrade/nova-blocks/issues/623) | Twelve frontend fixtures cover13 native overlay contexts and38 social style/override collections: dark, light, shifted, nested, inactive containers, named/custom explicit colors and unsignalled brand colors. A custom overlay template part retains its own colors. Eleven editor checks cover both editors, presets, one-step undo, an actual Info palette change and save/reload. Saved Info colors also match on the frontend. | [Frontend](results/623-front-results.json), [editors](results/623-editor-results.json), [saved palette](results/623-palette-frontend.json), [custom overlay](results/623-custom-part-results.json) |
| [#624](https://github.com/pixelgrade/nova-blocks/issues/624) | The existing fix was retested against the final source in ten frontend cases: static Solid, legacy hero transparency, Solid through sticky/unsticky, Solid/Transparent secondary clones, three mobile branding/menu variants, Patch and resize without reload. The Solid control and an additional load/stick/unstick regression were captured. Both editor undo/persistence/default-serialization tests passed in the original implementation verification on17 September. | [Current ten cases](results/624-current-full-results.json), [additional regression](results/624-sept18-regression-results.json) |

The final full `npm test` run passed in the commit hook (56.6s), including standalone PHP, native Node, Jest and compatibility Jest buckets. `npm run build` completed under Node22.22.0. [Artifact hashes](results/620-623-artifact-hashes.json) verify source/mirror parity and the HTTP-served runtime bundles. [Environment](environment.json) records the final clean mirror state.

The #623 dark screenshots were captured before the fixture was deliberately changed to the Info palette; both states are included. The #621 on/off screenshots were captured after actual theme-mod changes, with explicit policies sharing one header. Geometry tests treat authored image height as a maximum when available width requires proportional containment. Standalone Core Site Logo keeps its native width-based min/max constraints.

Automated regression contracts are committed with the source fixes. These artifacts document source verification; they are not a plugin release artifact.
