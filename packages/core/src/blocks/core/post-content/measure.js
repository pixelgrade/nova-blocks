/**
 * The authored content width of a Post Content block (GitHub #650): the
 * shared helper Group also uses (#635).
 */
export { getAuthoredContentSize as getPostContentMeasure } from '../content-measure';
